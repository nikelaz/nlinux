[package]
name = "web-app-install"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.2", features = ["derive"] }
dialoguer = "0.11"
reqwest = { version = "0.12", features = ["blocking", "json"] }
dirs = "5"
